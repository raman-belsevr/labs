from distutils.core import setup

setup(
    name = 'raspi',
    packages = ['raspi', 'raspi.fc', 'raspi.sensor'],
    version = '0.1',
    author = 'raman grover',
    author_email='raman@belsevr.com',
    url = 'blah')
