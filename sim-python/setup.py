from distutils.core import setup

setup(
    name = 'sim',
    packages = ['sim', 'sim.control', 'sim.server', 'sim.physics', 'sim.visual', 'sim.model', 'sim.physics.utils'],
    version = '0.1',
    author = 'raman griver',
    author_email='raman@belsevr.com',
    url = 'sim.belsevr')
